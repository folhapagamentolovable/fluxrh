import { resolvePayrollExceptionSchema } from "@fluxrh/contracts";import type { FastifyInstance } from "fastify";import { sendData } from "../../shared/http.js";import { InMemoryPayrollRepository } from "./payroll.repository.js";const repository=new InMemoryPayrollRepository();export async function payrollRoutes(app:FastifyInstance){app.get("/overview",async(_r,reply)=>sendData(reply,await repository.overview()));app.post<{Params:{employeeId:string;exceptionId:string}}>("/employees/:employeeId/exceptions/:exceptionId/resolve",async(req,reply)=>{const p=resolvePayrollExceptionSchema.safeParse(req.body);if(!p.success)return reply.code(400).send({error:"validation_error"});const value=await repository.resolve(req.params.employeeId,req.params.exceptionId);return value?sendData(reply,value):reply.code(404).send({error:"exception_not_found"})});app.post<{Params:{employeeId:string}}>("/employees/:employeeId/approve",async(req,reply)=>{const value=await repository.approve(req.params.employeeId);return value?sendData(reply,value):reply.code(422).send({error:"employee_has_open_exceptions"})});app.post("/close",async(_req,reply)=>{const result=await repository.close();return "error" in result?reply.code(422).send({error:result.error}):sendData(reply,result.data)})}
