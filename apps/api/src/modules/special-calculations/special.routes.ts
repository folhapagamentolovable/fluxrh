import { createSpecialCalculationSchema,resolveSpecialCalculationSchema } from "@fluxrh/contracts";import type { FastifyInstance } from "fastify";import { sendData } from "../../shared/http.js";import { InMemorySpecialRepository } from "./special.repository.js";const repository=new InMemorySpecialRepository();export async function specialCalculationRoutes(app:FastifyInstance){app.get("/overview",async(_r,reply)=>sendData(reply,await repository.overview()));app.post("/calculations",async(req,reply)=>{const p=createSpecialCalculationSchema.safeParse(req.body);if(!p.success)return reply.code(400).send({error:"validation_error",issues:p.error.issues});return sendData(reply,await repository.create(p.data),201)});app.post<{Params:{id:string;exceptionId:string}}>("/calculations/:id/exceptions/:exceptionId/resolve",async(req,reply)=>{const p=resolveSpecialCalculationSchema.safeParse(req.body);if(!p.success)return reply.code(400).send({error:"validation_error"});const value=await repository.resolve(req.params.id,req.params.exceptionId);return value?sendData(reply,value):reply.code(404).send({error:"exception_not_found"})});app.post<{Params:{id:string}}>("/calculations/:id/approve",async(req,reply)=>{const value=await repository.approve(req.params.id);return value?sendData(reply,value):reply.code(422).send({error:"open_exceptions"})})}
