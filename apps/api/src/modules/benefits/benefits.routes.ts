import { createBenefitEnrollmentSchema,createEmployeeMovementSchema,decideEmployeeMovementSchema } from "@fluxrh/contracts";import type { FastifyInstance } from "fastify";import { sendData } from "../../shared/http.js";import { InMemoryBenefitsRepository } from "./benefits.repository.js";const repository=new InMemoryBenefitsRepository();export async function benefitsRoutes(app:FastifyInstance){app.get("/overview",async(_r,reply)=>sendData(reply,await repository.overview()));app.post("/enrollments",async(req,reply)=>{const p=createBenefitEnrollmentSchema.safeParse(req.body);if(!p.success)return reply.code(400).send({error:"validation_error",issues:p.error.issues});const value=await repository.enroll(p.data);return value?sendData(reply,value,201):reply.code(404).send({error:"plan_not_found"})});app.post("/movements",async(req,reply)=>{const p=createEmployeeMovementSchema.safeParse(req.body);if(!p.success)return reply.code(400).send({error:"validation_error",issues:p.error.issues});return sendData(reply,await repository.createMovement(p.data),201)});app.post<{Params:{id:string}}>("/movements/:id/decision",async(req,reply)=>{const p=decideEmployeeMovementSchema.safeParse(req.body);if(!p.success)return reply.code(400).send({error:"validation_error"});const value=await repository.decideMovement(req.params.id,p.data.decision);return value?sendData(reply,value):reply.code(404).send({error:"movement_not_found"})})}
