import{describe,expect,it}from"vitest";import{InMemoryOccupationalRepository}from"./occupational.repository.js";describe("occupational health",()=>{it("releases a fit employee without clinical diagnosis data",async()=>{const repo=new InMemoryOccupationalRepository();const value=await repo.complete("exam_1",{result:"fit",responsiblePhysician:"Dra. Teste",asoDocumentId:"aso_test",validUntil:"2027-08-26"});expect(value?.workflowStatus).toBe("released");expect(value).not.toHaveProperty("diagnosis")});it("creates an exception and blocks workflow when employee is unfit",async()=>{const repo=new InMemoryOccupationalRepository();const value=await repo.complete("exam_1",{result:"unfit",responsiblePhysician:"Dra. Teste",asoDocumentId:"aso_unfit",validUntil:"2026-09-30"});expect(value?.workflowStatus).toBe("exception");const overview=await repo.overview();expect(overview.exceptions.some(x=>x.examId==="exam_1"&&x.status==="open")).toBe(true)});it("requires a functional restriction when result has restrictions",async()=>{const{completeOccupationalExamSchema}=await import("@fluxrh/contracts");expect(completeOccupationalExamSchema.safeParse({result:"fit_with_restrictions",responsiblePhysician:"Dra. Teste",asoDocumentId:"aso_1",validUntil:"2027-01-01"}).success).toBe(false)})});
